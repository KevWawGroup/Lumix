export const metadata = { title: "LUMIX Automation Engine", description: "WHEN → IF → THEN" };
export default function RootLayout({ children }) {
  return <html lang="en"><body style={{ margin: 0 }}>{children}</body></html>;
}
